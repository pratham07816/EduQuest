
  # Student Learning Interface

  This is a code bundle for Student Learning Interface. The original project is available at https://www.figma.com/design/AehhKDwuykDrf0KEKJz129/Student-Learning-Interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  